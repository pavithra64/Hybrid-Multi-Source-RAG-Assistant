version: "3.9"

services:

  rag-app:

    build: .

    container_name: hybrid-rag

    ports:
      - "8501:8501"

    restart: unless-stopped

    volumes:
      - ./data:/app/data
      - ./vectorstore:/app/vectorstore